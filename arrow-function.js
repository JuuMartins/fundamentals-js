// Arrow function
const apresentarArrow = nombre => `My name is ${nombre}`;

//Arrow function with more instructions
const somaNumeros = (num1, num2) => {
    if (num1 || num2 > 10) {
        return "Only numbers of 1 to 9"
    } else {
        return num1 + num2;
    }
}